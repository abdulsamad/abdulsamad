---
import type { HTMLAttributes } from 'astro/types';

export interface Props extends HTMLAttributes<'section'> {}

const { ...props } = Astro.props;
---

<section
  class={`relative px-[4vw] w-full max-w-[1920px] mx-auto ${props.class}`}
  {...props}>
  <slot />
</section>
